describe('Email Sender Test', () => {
    describe('Test mail for 2 remaining items function', () => {
        it('Should return true when calling', () => {
            expect(sendMailFor2RemainingItems().toEquals(true);
        })
    })
})
